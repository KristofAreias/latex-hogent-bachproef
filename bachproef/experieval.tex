\chapter{\IfLanguageName{dutch}{Experimentele evaluatie}{Experimental evaluation}}%
\label{ch:experieval}

In dit hoofdstuk wordt de experimentele evaluatie van de proof-of-concept besproken. De focus ligt op het analyseren van de impact van verschillende modelcomponenten en hun onderlinge gewichten. Daarnaast worden de belangrijkste problemen en beperkingen van het systeem in detail beschreven.

\section{Invloed van modelgewichten}

Tijdens de experimenten werd onderzocht hoe belangrijk elke component in de pipeline is voor het eindresultaat. Hierbij werd specifiek gekeken naar de bijdrage van drie onderdelen:
\begin{itemize}
    \item CLIP (visuele embedding)
    \item OCR via Florence-2 (tekstembedding)
    \item Florence-2 visual (visuele embedding)
\end{itemize}

Voor elk van deze componenten werd een gewicht ingesteld. Deze gewichten bepalen hoeveel invloed een bepaalde embedding heeft in de uiteindelijke gecombineerde vector.

\begin{table}[H]
\centering
\caption{Invloed van verschillende gewichtsconfiguraties op product retrieval similarity.}
\label{tab:fusion_weights}
\begin{tabular}{lcccc}
\toprule
\textbf{Configuratie} & \textbf{CLIP} & \textbf{OCR} & \textbf{FL2} & \textbf{Similarity} \\
\midrule
Equal fusion                       & 0.33 & 0.33 & 0.33 & 0.6690 \\

CLIP-dominant                      & 0.70 & 0.20 & 0.10 & 0.6922 \\
CLIP-dominant                      & 0.60 & 0.25 & 0.15 & 0.7160 \\
CLIP-dominant                      & 0.50 & 0.30 & 0.20 & 0.7271 \\

OCR-dominant                       & 0.20 & 0.70 & 0.10 & 0.6723 \\
OCR-dominant                       & 0.25 & 0.60 & 0.15 & 0.6985 \\
OCR-dominant                       & 0.30 & 0.50 & 0.20 & 0.7147 \\

FL2-dominant                       & 0.20 & 0.10 & 0.70 & 0.3283 \\
FL2-dominant                       & 0.25 & 0.15 & 0.60 & 0.4210 \\
FL2-dominant                       & 0.30 & 0.20 & 0.50 & 0.5206 \\

CLIP + OCR                         & 0.55 & 0.45 & 0.00 & 0.7431 \\
CLIP + OCR                         & 0.45 & 0.55 & 0.00 & 0.7377 \\
\textbf{CLIP + OCR}                & \textbf{0.50} & \textbf{0.50} & \textbf{0.00} & \textbf{0.7433} \\

CLIP + FL2                         & 0.55 & 0.00 & 0.45 & 0.5267 \\
CLIP + FL2                         & 0.50 & 0.00 & 0.50 & 0.4953 \\
CLIP + FL2                         & 0.60 & 0.00 & 0.40 & 0.5525 \\

OCR + FL2                          & 0.00 & 0.55 & 0.45 & 0.5164 \\
OCR + FL2                          & 0.00 & 0.50 & 0.50 & 0.4870 \\
OCR + FL2                          & 0.00 & 0.45 & 0.55 & 0.4528 \\

Alleen CLIP                        & 1.00 & 0.00 & 0.00 & 0.5921 \\
Alleen OCR                         & 0.00 & 1.00 & 0.00 & 0.5709 \\
Alleen FL2                         & 0.00 & 0.00 & 1.00 & 0.1329 \\
\bottomrule
\end{tabular}
\end{table}

Door deze gewichten systematisch aan te passen, kon geobserveerd worden welke combinatie de beste resultaten opleverde.

Uit deze experimenten bleek dat de visuele component van Florence-2 weinig tot geen positieve impact had. In veel gevallen zorgde deze zelfs voor slechtere resultaten. Hierdoor werd besloten om Florence-2 visual volledig weg te laten in de uiteindelijke configuratie.

De beste resultaten werden bekomen met een combinatie van CLIP en OCR. Hierbij werd een gelijke verdeling gebruikt. Concreet gaf een configuratie met CLIP op 0.50 en OCR op 0.50 de meest consistente resultaten.

Dit toont aan dat tekstinformatie een zeer belangrijke rol speelt in productherkenning. Vooral bij producten met duidelijke merk- of productnamen op de verpakking helpt OCR om betere matches te vinden.

\subsection{Finetuning van CLIP}

Daarnaast werd ook geëxperimenteerd met finetuning van het CLIP-model. Hierbij werden verschillende batchgroottes, learning rates en aantallen epochs getest. Hoewel de configuraties verbeteringen opleverden, presteerde het gefinetunede model nog steeds onder de prestatievereisten (Tabel~\ref{tab:clip_evaluatie}).

Een mogelijke verklaring hiervoor is dat CLIP reeds getraind is op een zeer grote hoeveelheid internetdata en daardoor sterke algemene representaties geleerd heeft. De gebruikte retaildataset was relatief klein en bevatte bovendien ruis en inconsistenties, waardoor het moeilijk was om het model verder te verbeteren zonder bestaande kennis te verstoren.

Wel bleek dat preprocessing van productnamen een duidelijke positieve impact had. Door tekst te normaliseren, bijvoorbeeld via lowercase-conversie en het verwijderen van speciale karakters, ontstonden consistentere embeddings en stabielere similarity scores.

\section{Analyse van resultaten}

Bij het analyseren van de resultaten werd voornamelijk gekeken naar de top-5 matches die door het systeem worden teruggegeven.

In veel gevallen bevat deze top-5 lijst correcte producten, maar de volgorde is niet altijd stabiel. 

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=0.5cm] 
        \node[inner sep=0pt] (img0) at (0,0)
            {\includegraphics[width=0.9\textwidth]{C:/Users/krist/Downloads/crop4_top5.jpg}};
            
        \node[inner sep=0pt, below=of img0] (img1)
            {\includegraphics[width=0.9\textwidth]{C:/Users/krist/Downloads/crop146_top5.png}};

    \end{tikzpicture}
    \caption{De top 5 van 2 gecropte cola-flessen wordt bekeken. Boven goede voorspellingen maar onder verschillen ze meer en staat de correcte match niet in de lijst. (v.l.n.r. crop4\_top5, crop24\_top5).}
    \label{fig:top5_problemen}
\end{figure}

De similarity scores van de top-5 liggen vaak zeer dicht bij elkaar. Dit zorgt ervoor dat kleine veranderingen in de input kunnen leiden tot een andere top-1 voorspelling.

Dit probleem is duidelijk zichtbaar bij producten zoals cola-flessen. Wanneer een afbeelding bijvoorbeeld een standaard Coca-Cola fles bevat, verschijnen in de top-5 vaak ook varianten zoals Coca-Cola Zero.

Een klein detail, zoals een grijze kleur op de verpakking van Coca-Cola Zero die naast een gewone Coca-Cola fles staat, kan een grote invloed hebben op de embedding. Wanneer een klein stukje van een ander product zichtbaar is in de crop, kan dit de matching verstoren.

Omgekeerd gebeurt hetzelfde. Een Coca-Cola Zero fles kan soms gematcht worden met een gewone Coca-Cola. Dit komt doordat de algemene vorm en tekst zeer gelijkaardig zijn, en soms een rode kleur mee gecropt wordt.

Daarnaast speelt taal ook een rol. Productnamen verschillen soms per regio. Zo kan ``Coca-Cola Zero'' ook aangeduid worden als ``Coke Zero''. OCR kan deze variaties oppikken, maar dit zorgt niet altijd voor consistente embeddings.

\section{Problemen met de dataset}

Een deel van de problemen kan gelinkt worden aan de dataset. Omdat de gebruikte dataset afkomstig is van OpenFoodFacts en bestaat uit uploads van verschillende gebruikers, varieert de datakwaliteit sterk. 

Dit heeft zeker ook positieve gevolgen omdat de variatie in beelden en productnamen het model kan helpen om robuustere representaties te leren. 

De grote problemen die voorkomen in de dataset zijn:
\begin{itemize}
    \item Sommige afbeeldingen zijn onherkenbaar doordat ze te wazig of te donker zijn
    \item In enkele gevallen hoort de afbeelding zelfs niet bij het juiste product
\end{itemize}

De afbeeldingen van de producten van OpenFoodFacts zijn URL's die dynamisch opgebouwd worden aan de hand van de metadata van dit product uit de dataset. De URL wordt samengesteld door de productcode (code), type afbeelding (key), revisie (rev) en resolutie (size) van het product. 

Wanneer er een fout zit in deze metadata, bijvoorbeeld een verkeerde code of key, kan dit leiden tot een afbeelding die niet overeenkomt met het daadwerkelijke product.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=0.5cm] 
        \node[inner sep=0pt] (img0) at (0,0)
            {\includegraphics[width=0.2\textwidth]{C:/Users/krist/Downloads/foute_foto_vb.jpg}};
            
        \node[inner sep=0pt, right=of img0] (img1)
            {\includegraphics[width=0.3\textwidth]{C:/Users/krist/Downloads/foute_foto_vb2.jpg}};

        \node[inner sep=0pt, right=2.5cm of img1] (img2)
            {\includegraphics[width=0.3\textwidth]{C:/Users/krist/Downloads/donker_wazig_vb.jpg}};
        
    \end{tikzpicture}
    \caption{Inconsistenties in de dataset OpenFoodFacts. Foute productafbeeldingen onder een product links, en rechts voorbeeld van onherkenbare product (v.l.n.r. foute\_foto\_vb, foute\_foto\_vb2, donker\_wazig\_vb).}
    \label{fig:inconsistenties_openfoodfacts}
\end{figure}

Deze inconsistenties maken het moeilijk voor het model om consistente embeddings te leren. Wanneer de referentiebeelden zelf niet betrouwbaar zijn, wordt het ook moeilijk om correcte matches te vinden.


\section{Problemen met objectdetectie}

Naast de dataset speelt ook de detectiestap een belangrijke rol in de fouten, maar deze konden geminimaliseerd worden.

Zowel YOLOv8n als YOLOv11s basis modellen toonden inconsistent gedrag bij het detecteren van producten. Vooral bij afbeeldingen die onder een hoek genomen zijn, werden vaak enkel de producten die zich dicht bij de camera bevinden correct gedetecteerd.

Producten die verder naar achter staan of gedeeltelijk verborgen zijn, werden vaak gemist. Dit zorgde ervoor dat een hoop producten in een afbeelding niet verwerkt wordt. Na finetuning op de SKU-110K dataset werd dit probleem quasi opgelost, maar het blijft een uitdaging bij bepaalde hoeken of slechte beeldkwaliteit.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=0.5cm] 
        \node[inner sep=0pt] (img0) at (0,0)
            {\includegraphics[width=0.5\textwidth]{C:/Users/krist/OneDrive/Documents/HoGent IT/BachproefHoGent/latex-hogent-bachproef/bachproef/test_17_comparison.jpg}};

        \node[inner sep=0pt, right=of img0] (img1)
            {\includegraphics[width=0.475\textwidth]{C:/Users/krist/OneDrive/Documents/HoGent IT/BachproefHoGent/latex-hogent-bachproef/bachproef/test_18_comparison.jpg}};
        
    \end{tikzpicture}
    \caption{Deze voorbeelden tonen het verschil in detectieprestaties na finetuning op de SKU-110K dataset (v.l.n.r. test\_17\_comparison, test\_18\_comparison).}
    \label{fig:belang_fintuning_yolo}
\end{figure}

Daarnaast zijn de bounding boxes niet altijd nauwkeurig. Omdat de crops rechthoekig zijn, bevatten ze vaak ook delen van naburige producten. De producten hun achtergrond verwijderen werd geprobeerd maar was enorm inconsistent. Volledige producten werden verwijderd of slechts kleine stukjes werden ervan over gehouden. Dit werd reeds besproken in \ref{subsec:beperkingen_detectie}.

Wanneer deze naburige producten een andere kleur of vorm hebben, beïnvloedt dit de embedding sterk. Zelfs een klein stuk van een ander product kan de vectorrepresentatie veranderen.

Dit probleem komt vaak voor bij dicht gevulde schappen waar producten tegen elkaar staan.

\subsection{Belang van domeinspecifieke finetuning}

De resultaten uit Tabel~\ref{tab:evaluatie_yolo} tonen duidelijk aan dat domeinspecifieke finetuning essentieel is voor productdetectie in retailomgevingen.

Winkelschappen bevatten vaak honderden kleine objecten die dicht bij elkaar staan en elkaar gedeeltelijk overlappen. Generieke objectdetectiemodellen zijn hier niet specifiek voor ontworpen, waardoor veel producten gemist worden.

Door finetuning op de SKU-110K dataset leerde YOLO11s beter omgaan met deze hoge productdichtheid en complexe winkelomgevingen. Dit leidde niet alleen tot een hoger aantal detecties, maar ook tot consistenter gecropte producten.

Deze verbetering heeft een directe impact op de rest van de pipeline. Wanneer producten niet correct gedetecteerd worden, kunnen er geen embeddings gegenereerd worden en is retrieval onmogelijk. Objectdetectie vormt daarom een fundamentele stap binnen het volledige systeem.

\section{Problemen met OCR}

OCR vormt een extra bron van fouten binnen het systeem.

Hoewel tekstinformatie zeer waardevol is, is de kwaliteit van OCR afhankelijk van de leesbaarheid van de verpakking. Wanneer tekst vervormd, verkreukeld of slecht zichtbaar is, kan de OCR foutieve output genereren.

Voorbeelden van problemen:
\begin{itemize}
    \item Verkeerd herkende letters
    \item Onvolledige woorden
\end{itemize}

Deze fouten kunnen dan rechtstreeks doorgegeven worden aan de text encoder van CLIP, wat resulteert in een iets wat afwijkende embedding.

Omdat OCR een relatief groot gewicht krijgt in de uiteindelijke combinatie, kunnen deze fouten een sterke impact hebben op het eindresultaat.

\section{Algemene beperkingen}

Een belangrijk inzicht uit de experimenten is dat geen van de gebruikte modellen specifiek ontworpen is voor deze toepassing.

CLIP is getraind op algemene internetdata \cite{DBLP:journals/corr/abs-2103-00020} en is niet geoptimaliseerd voor productherkenning op SKU-niveau. Florence-2 is een algemeen vision-language model en heeft geen specifieke kennis van retailproducten \cite{xiao2023florence2advancingunifiedrepresentation}. YOLO-modellen zijn niet voldoende getraind op datasets met hoge productdensiteit zoals winkelschappen.

Deze mismatch tussen trainingsdata en toepassing verklaart een groot deel van de inconsistenties in de resultaten.

Het systeem werkt in veel gevallen redelijk goed, maar kleine veranderingen in input kunnen grote verschillen veroorzaken in output. Dit maakt het systeem gevoelig voor ruis en minder betrouwbaar in realistische scenario's.

De combinatie van datasetproblemen, detectiefouten en vooral modelbeperkingen zorgt ervoor dat de resultaten niet altijd stabiel zijn.